<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vn.vbeeon" >

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:name=".VBeeOnApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".presentation.activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >

        </activity>
        <activity android:name=".presentation.activity.FragmentActivity"/>

        <activity android:name=".presentation.activity.IntroduceActivity"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".presentation.activity.SphygmomanometerActivity"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".presentation.activity.ConnectionActivity"/>

        <activity
            android:screenOrientation="portrait"
            android:name=".presentation.activity.DeviceAddNewActivity"/>

        <activity android:name=".presentation.activity.SplashScreenAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>